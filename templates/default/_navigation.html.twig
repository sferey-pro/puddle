{% set _route = app.current_route %}
  <div class="container-xl">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbar-menu" aria-controls="navbar-menu"
      aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <!-- BEGIN NAVBAR LOGO -->
    <a href="{{ path('homepage') }}" class="navbar-brand navbar-brand-autodark me-3">
        <img src="{{ asset('static/logo.png') }}" class="navbar-brand-image" alt="" width="110" height="110">
    </a>
    <!-- END NAVBAR LOGO -->
    <ul class="navbar-nav">
      <li class="nav-item{{ _route == 'homepage' ? ' active' : '' }}">
        <a class="nav-link" href="{{ path('homepage') }}">
          <span class="nav-link-icon">
            <twig:ux:icon name="tabler:home" />
          </span>
          <span class="nav-link-title"> Home </span>
        </a>
      </li>
      <li class="nav-item{{ _route == 'profile' ? ' active' : '' }}">
        <a class="nav-link" href="{{ path('profile') }}">
          <span class="nav-link-icon">
            <twig:ux:icon name="tabler:user" />
          </span>
          <span class="nav-link-title"> Profile </span>
        </a>
      </li>
      <li class="nav-item{{ _route == 'settings' ? ' active' : '' }}">
        <a class="nav-link" href="{{ path('settings') }}">
          <span class="nav-link-icon">
            <twig:ux:icon name="tabler:settings" />
          </span>
          <span class="nav-link-title"> Settings </span>
        </a>
      </li>
    </ul>
    <div class="navbar-nav flex-row order-md-last ms-auto">
      <div class="nav-item dropdown">
        <a href="#" class="nav-link d-flex lh-1 text-reset"
          data-bs-toggle="dropdown" aria-label="Open user menu">
          <span class="avatar avatar-sm">SF</span>
          <div class="d-none d-xl-block ps-2">
            <div>Stéphane FEREY</div>
            <div class="mt-1 small text-secondary">Admin</div>
          </div>
        </a>
        <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
          <a href="#" class="dropdown-item">Status</a>
          <a href="{{ path('profile') }}" class="dropdown-item">Profile</a>
          <a href="#" class="dropdown-item">Feedback</a>
          <div class="dropdown-divider"></div>
          <a href="{{ path('settings') }}" class="dropdown-item">Settings</a>
          <a href="{{ path('logout') }}" class="dropdown-item">Logout</a>
        </div>
      </div>
    </div>
  </div>
