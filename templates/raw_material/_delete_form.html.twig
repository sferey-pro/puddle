<formc lass="form-inline" method="post" action="{{ path('app_raw_material_delete', {'id': raw_material.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ raw_material.id) }}">
    <button class="btn btn-block btn-danger"><twig:ux:icon name="trash"/> {{ 'action.delete_raw_material'|trans }}</button>
</form>
