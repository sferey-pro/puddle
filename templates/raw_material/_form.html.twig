{{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">
        <twig:ux:icon name="tabler:save-changes"/> {{ button_label|default('label.create_raw_material'|trans) }}
    </button>

    <twig:Button:ActionItem
        withBackToList="app_raw_material_index"
    />
{{ form_end(form) }}
