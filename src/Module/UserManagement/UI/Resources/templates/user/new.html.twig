{% extends 'layout.html.twig' %}

{% block title %}New User{% endblock %}

{% block body_id 'user_create' %}

{% block main %}
    <twig:Semantic:Title title="{{ 'user.index'|trans }}" />
    <div class="page-body">
        <div class="container-xl">
            <div class="row row-cards">
                <div class="col-md-6">
                    <twig:UserManagement:UserForm />
                </div>
            </div>
        </div>
    </div>
{% endblock %}
