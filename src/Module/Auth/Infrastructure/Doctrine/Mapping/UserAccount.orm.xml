<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:gedmo="http://gediminasm.org/schemas/orm/doctrine-extensions-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                          https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Module\Auth\Domain\UserAccount" table="`auth_user_accounts`">
        <id name="id" type="user_id" column="id" length="36" />

        <field name="password" type="password" column="password" nullable="true" />
        <field name="email" type="email_address" column="email" nullable="true" />
        <field name="roles" type="json" />

        <field name="verified" type="boolean" column="is_verified" nullable="false">
            <options>
                <option name="default">false</option>
            </options>
        </field>
        <field name="active" type="boolean" column="is_active" nullable="false">
            <options>
                <option name="default">true</option>
            </options>
        </field>

        <field name="firstLoginAt" type="datetime_immutable" nullable="true" />
        <field name="createdAt" type="datetime_immutable">
            <gedmo:timestampable on="create"/>
        </field>
        <field name="updatedAt" type="datetime_immutable">
            <gedmo:timestampable on="update"/>
        </field>

        <unique-constraints>
            <unique-constraint columns="email" name="UNIQ_IDENTIFIER_AUTH_USERS_EMAIL" />
        </unique-constraints>
    </entity>

</doctrine-mapping>
