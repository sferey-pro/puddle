<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                          https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
    ==================== MAGIC LINK CREDENTIAL ====================

    Credential pour authentification par lien magique (email).
    Hérite de AccessCredentialInterface via SINGLE_TABLE.
    -->

    <entity name="Authentication\Domain\Model\AccessCredential\MagicLinkCredential">

        <!-- ==================== PROPRIÉTÉS SPÉCIFIQUES ==================== -->

        <!-- Token unique pour le magic link -->
        <field name="token" type="magic_link_token" column="token" length="255" nullable="true" unique="true">
            <options>
                <option name="comment">Unique token for magic link</option>
            </options>
        </field>
    </entity>

</doctrine-mapping>
