<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                          https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!--
    ==================== OTP CREDENTIAL ====================

    Credential pour authentification par code OTP (SMS).
    -->

    <entity name="Authentication\Domain\Model\AccessCredential\OTPCredential">

        <!-- ==================== PROPRIÉTÉS SPÉCIFIQUES ==================== -->

        <!-- Code OTP à 6 chiffres -->
        <field name="token" type="otp_code" column="code" length="6" nullable="true">
            <options>
                <option name="comment">6-digit OTP code</option>
                <option name="fixed">true</option>
            </options>
        </field>

        <!-- Nombre de tentatives -->
        <field name="attempts" type="integer" column="attempts" nullable="false">
            <options>
                <option name="default">0</option>
                <option name="comment">Number of verification attempts</option>
            </options>
        </field>
    </entity>

</doctrine-mapping>
